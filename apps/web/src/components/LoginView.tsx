import { useState } from 'react';
import { LockKeyhole } from 'lucide-react';
import { api } from '../lib/api';
export function LoginView({onLogin}:{onLogin:(u:any)=>void}){const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await api.login(String(f.get('email')||''),String(f.get('password')||''));onLogin(r.user)}catch(x){setError(x instanceof Error?x.message:'Connexion impossible')}finally{setBusy(false)}}return <main className="login-page"><section className="login-card"><div className="login-brand login-brand-centered"><img className="vigie-eye-login" src="/vigie-eye.svg" alt="Vigie"/></div><div className="login-icon"><LockKeyhole size={24}/></div><h1>Connexion à Vigie</h1><p>Accès réservé aux utilisateurs habilités. Les données affichées dépendent de votre périmètre d’agence comptable.</p><form onSubmit={submit}><label>Adresse électronique</label><input name="email" type="email" autoComplete="username" required autoFocus/><label>Mot de passe</label><input name="password" type="password" autoComplete="current-password" required/>{error&&<div className="login-error" role="alert">{error}</div>}<button className="btn-primary-v" disabled={busy}>{busy?'Connexion…':'Se connecter'}</button></form></section></main>}
