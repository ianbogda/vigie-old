import { Upload, X } from 'lucide-react';
import type { Eple } from '../types/dashboard';
export function ImportModal({ result, onClose, onSubmit, establishments, selectedId }: { result:any; onClose:()=>void; onSubmit:(e:React.FormEvent<HTMLFormElement>)=>void; establishments:Eple[]; selectedId:string }) { const selectedEple=establishments.find(e=>e.id===selectedId); const selected=selectedEple?.opaleEntity||''; return <div className="modalveil" onClick={onClose}><div className="importbox" onClick={e=>e.stopPropagation()}><button className="x" onClick={onClose}><X/></button><Upload size={28}/><h3>Importer une situation Op@le</h3><p>Choisis l’ETS cible. Pour les données comptables (classes 1 à 8), YGPIE1 et le budget .xlsx, Vigie contrôle la cohérence de l’ETS lorsque le fichier le précise ; sinon, l’ETS sélectionné sert de contexte de rattachement.</p><form onSubmit={onSubmit}><label>ETS de rattachement</label><select name="ets" defaultValue={selected} required><option value="">Choisir un ETS…</option>{establishments.filter(e=>e.opaleEntity).map(e=><option key={e.id} value={e.opaleEntity!}>{e.name} · {e.opaleEntity}</option>)}</select><input name="file" type="file" accept=".lis,.csv,.xlsx" required/><label>Exercice YCONSDEP / YCONSREC</label><select name="exercise" defaultValue={new Date().getFullYear()}>{Array.from({length:8},(_,i)=>new Date().getFullYear()+1-i).map(y=><option key={y} value={y}>{y}</option>)}</select><small className="import-hint">Utilisé uniquement pour YCONSDEP/YCONSREC, car l’exercice n’est pas porté de façon fiable par le fichier.</small><button className="btn-primary-v">Analyser et importer</button></form>{result&&<div className="result">{result.loading?'Analyse en cours…':result.error?`Import refusé : ${result.error}`:result.type==='ygpie1'?`YGPIE1 · ${result.control?.importedRows||0} pièce(s) non soldée(s) importée(s)`:result.type==='accounting'?`Comptabilité · ${result.control?.addedRows||0} ajoutée(s) · ${result.control?.updatedRows||0} actualisée(s) · ${result.control?.unchangedRows||0} inchangée(s)${result.control?.etsFromContext?' · ETS rattaché par le contexte':''}`:`${result.type} · ${result.control?.importedRows||0} lignes importées`}</div>}</div></div> }
